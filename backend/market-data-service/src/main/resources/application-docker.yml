spring:
  cloud:
    consul:
      host: consul
      discovery:
        enabled: true
        register: true
  data:
    redis:
      host: redis
  kafka:
    bootstrap-servers: kafka:9092

management:
  health:
    consul:
      enabled: false
    discovery:
      enabled: false
